{
  "name": "pontmore-escrow-service",
  "version": "1.0.0",
  "description": "Pontmore PIP-01 conformant custodial escrow service over HTTPS with NIP-98 auth",
  "main": "server.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "echo No build step required",
    "publish": "node scripts/publish-descriptor.js --publish"
  },
  "dependencies": {
    "@noble/curves": "^1.6.0",
    "@noble/hashes": "^1.5.0",
    "@scure/base": "^1.1.9",
    "@supabase/supabase-js": "^2.43.4",
    "express": "^4.19.2",
    "node-cron": "^4.2.1",
    "ws": "^8.21.1"
  },
  "devDependencies": {
    "dotenv": "^16.4.5"
  }
}
